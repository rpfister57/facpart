% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radial-ellipse.R
\name{radialEllipse}
\alias{radialEllipse}
\title{Binary radial-elliptic partition (one separating ellipse)}
\usage{
radialEllipse(
  crd,
  group,
  fill = FALSE,
  output = TRUE,
  col = "purple",
  cols = c("steelblue", "tomato"),
  lwd = 2,
  lty = 1
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns.}

\item{group}{Factor with exactly 2 levels.}

\item{fill}{If \code{TRUE}, shade inner ellipse and outer region.}

\item{output}{If \code{TRUE} (default), return results list.}

\item{col}{Ellipse border colour (default \code{"purple"}).}

\item{cols}{Length-2 fill colours (default \code{c("steelblue", "tomato")}).}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}
}
\value{
If \code{output = TRUE}, a list with \code{cx}, \code{cy}, \code{a}, \code{b}, \code{angle}
(radians), \code{misclass} (list with \code{n} and \code{indices}), \code{sector}, and
\code{majority}.
}
\description{
Finds the ellipse minimising misclassification between two groups of 2D
points. All 5 parameters (cx, cy, a, b, angle) are found by Nelder-Mead,
starting from the covariance ellipse of each group in turn; the ordering
with the lower misclassification is retained.
}
\examples{
\dontrun{
set.seed(1)
inner <- cbind(rnorm(20, 0, 0.3), rnorm(20, 0, 0.2))
th    <- runif(20, 0, 2 * pi)
outer <- cbind(2.0 * cos(th), 1.2 * sin(th)) + matrix(rnorm(40, 0, 0.1), 20)
crd <- rbind(inner, outer)
grp <- factor(c(rep("in", 20), rep("out", 20)))
plot(crd, asp = 1)
radialEllipse(crd, grp, fill = TRUE)
}

}
