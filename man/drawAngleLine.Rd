% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{drawAngleLine}
\alias{drawAngleLine}
\title{Draw a line of given angle and length from a starting point}
\usage{
drawAngleLine(x = 0, y = 0, angle, len = 1, ppoint = FALSE, ...)
}
\arguments{
\item{x, y}{Coordinates of the starting point (default \code{0}, \code{0}).}

\item{angle}{Direction of the line in radians.}

\item{len}{Length of the line (default \code{1}).}

\item{ppoint}{Logical: Should the endpoint be marked with a filled
point (default \code{FALSE}).}

\item{...}{Further graphical parameters passed to
\code{\link[graphics:segments]{graphics::segments()}}, e.g. \code{col}, \code{lwd}, \code{lty}.}
}
\value{
\code{invisible(NULL)}, called for its side effect of drawing on the
active plot.
}
\description{
On the currently active plot, draws a straight line starting at
\verb{(x, y)} running in the direction \code{angle} (in \strong{radians}, counter-clockwise
from the positive x-axis) with length \code{len}. Optionally marks the
endpoint with a point. Useful for sketching cut rays of an angular
partition (see \code{\link[=angularPartition]{angularPartition()}}) or any other direction indicator.
}
\examples{
\dontrun{
plot(-2:2, -2:2, type = "n", asp = 1)
drawAngleLine(angle = pi / 4, len = 2, ppoint = TRUE, col = "blue")
drawAngleLine(x = -1, y = -1, angle = 0, len = 1.5, lty = 2)
}

}
