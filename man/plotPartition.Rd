% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plotPartition}
\alias{plotPartition}
\title{Replot a stored partition result}
\usage{
plotPartition(
  Pout,
  fill = FALSE,
  cols = NULL,
  col = NULL,
  lwd = 2,
  lty = 1,
  highlight = FALSE,
  add = FALSE
)
}
\arguments{
\item{Pout}{A result list from one of the partition functions above (must
have a \code{partition} field naming a known family
("axial", "angular", "circle", "ellipse"), plus \code{pcoords}/\code{pgroup}).}

\item{fill}{If \code{TRUE}, shade the regions between boundaries (default
\code{FALSE}).}

\item{cols}{Fill colours; auto-generated if \code{NULL} -- \code{c("steelblue", "tomato")} for a 2-region partition, \code{hcl.colors(k, "Pastel 1")}
otherwise, matching the palette the originating partition function would
have used by default. A char vector with k valid colors can be supplied.}

\item{col}{Boundary line/ray colour; if \code{NULL} (default), \code{"darkorange"}
for \code{partition == "angular"} and \code{"purple"} otherwise, matching the
originating functions' own defaults.}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{highlight}{If \code{TRUE}, overlay a marker on every misclassified point
via \code{\link[=highlightMisclass]{highlightMisclass()}} (default \code{FALSE}).}

\item{add}{If \code{FALSE} (default), start a fresh plot of \code{Pout$pcoords} with
group labels before drawing the boundaries; if \code{TRUE}, draw only the
boundaries (and optional fill/highlight) on the already-active plot.}
}
\value{
\code{invisible(NULL)}, called for its side effect of drawing on the
active plot.
}
\description{
Redraws the configuration and partitions from the output list returned by
\code{\link[=axialLine]{axialLine()}}, \code{\link[=axialLines]{axialLines()}}, \code{\link[=radialCircle]{radialCircle()}}, \code{\link[=radialCircles]{radialCircles()}},
\code{\link[=radialEllipse]{radialEllipse()}}, \code{\link[=radialEllipses]{radialEllipses()}} or \code{\link[=angularPartition]{angularPartition()}}. It is a
simple replot of the stored partition regions with no recomputation.
}
\examples{
\dontrun{
set.seed(1)
crd <- rbind(cbind(rnorm(15, -1), rnorm(15)), cbind(rnorm(15, 1), rnorm(15)))
grp <- factor(c(rep("a", 15), rep("b", 15)))
res <- radialCircles(crd, grp, output = TRUE, add = FALSE)
plotPartition(res, fill = TRUE, highlight = TRUE)
}

}
