% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radial-circle.R
\name{radialCircle}
\alias{radialCircle}
\title{Binary radial partition (one separating circle)}
\usage{
radialCircle(
  crd,
  group,
  cx = NULL,
  cy = NULL,
  fill = FALSE,
  output = TRUE,
  col = "purple",
  cols = c("steelblue", "tomato"),
  lwd = 2,
  lty = 1,
  .method = "Nelder-Mead",
  add = TRUE
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns.}

\item{group}{Factor with exactly 2 levels.}

\item{cx, cy}{Center of the separating circle; optimised when \code{NULL} (default).}

\item{fill}{If \code{TRUE}, shade the inner disc and outer region (default \code{FALSE}).}

\item{output}{If \code{TRUE} (default), return results list.}

\item{col}{Circle border colour (default \code{"purple"}).}

\item{cols}{Length-2 fill colours (default \code{c("steelblue", "tomato")}).}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{.method}{\code{"Nelder-Mead"} (default) or \code{"SANN"}.}

\item{add}{If \code{TRUE} (default), add to existing plot; if \code{FALSE}, call
\code{plot()} first.}
}
\value{
If \code{output = TRUE}, a list with \code{center}, \code{radius}, \code{misclass}
(integer), \code{misclass_points} (data frame with columns \code{x}, \code{y}, \code{label}
for each misclassified point), \code{sector} (\code{1} inside / \code{2} outside per
point), and \code{majority} (\code{character[2]}).
}
\description{
Finds the circle minimising misclassification between two groups of 2D
points. The center is found by multi-start Nelder-Mead (starts: data
centroid plus each group's centroid) unless \code{cx} and \code{cy} are supplied.
}
\details{
\strong{Search at fixed center.} Points are sorted by distance from the center
and every realisable radius is scanned: the inside/outside split is scored
by its exact bijection-constrained misclassification (the better of the
two ways to match the two regions to the two groups) — the same criterion
used to derive \code{sector}/\code{majority} below, so the search targets exactly
the quantity reported as \code{misclass}, and neither group is assumed to be
the inner one. Radii that separate coincident distances are skipped as
unrealisable. The scan also considers the degenerate radii that leave one
region empty; when the configuration has no radial structure these can be
the true minimum, so they are allowed, but on a tie a circle that really
does split the points is preferred. \code{radialCircles()} with \code{k = 2} and the
same center returns the same circle.
}
\examples{
set.seed(1)
inner <- cbind(rnorm(20, 0, 0.3), rnorm(20, 0, 0.3))
th <- runif(20, 0, 2 * pi)
outer <- cbind(2 * cos(th), 2 * sin(th)) + matrix(rnorm(40, 0, 0.1), 20)
crd <- rbind(inner, outer)
grp <- factor(c(rep("in", 20), rep("out", 20)))
radialCircle(crd, grp, fill = TRUE, add = FALSE)

}
