% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/angular.R
\name{angularPartition}
\alias{angularPartition}
\title{Angular k-way (wedge) partition of a 2D configuration}
\usage{
angularPartition(
  crd,
  group,
  cx = NULL,
  cy = NULL,
  output = TRUE,
  col = "darkorange",
  lwd = 2,
  lty = 1,
  add = TRUE
)
}
\arguments{
\item{crd}{Numeric matrix or numeric data frame with exactly 2 columns; no NAs.}

\item{group}{Factor with \verb{n >= k >= 2} levels, same length as nrow of crd.}

\item{cx, cy}{Center; optimised when either is \code{NULL}.}

\item{output}{If \code{TRUE} (default), return list of results.}

\item{col}{Ray colour (default \code{"darkorange"}).}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{add}{If \code{TRUE} (default), add to existing plot, else plot configuration.}
}
\value{
If \code{output = TRUE}, a list with:
\itemize{
\item \code{cuts} — \code{numeric[k]}, cut angles of rays (in radians).
\item \code{margin} — minimum distance from any cut ray to nearest point.
\item \code{misclass} — integer, number of misclassified points.
\item \code{misclass_points} — data frame (\code{x}, \code{y}, \code{label}) of misclassified points
\item \code{sector} — \code{integer[n]}, assigned sector \verb{1..k} per point
\item \code{majority} — \code{character[k]}, majority group per sector
\item \code{center} — \code{c(cx, cy)} center coordinates of rays
\item \code{pt_angles} — angles (in radians) of points from center
}
}
\description{
Finds \code{k} rays (straight lines) emanating from a center point that partition
a 2D configuration of \code{n} points into \code{k} angular sectors, minimising total
misclassification (points whose group differs from the majority group
in their sector), and draws the lines. (see: Shye, S. (2014). Faceted Smallest Space Analysis (FSSA).
In A. Michalos (Ed.), Encyclopedia of quality of life research (pp. 2129-2133).
New York: Springer.)
}
\details{
\strong{Search at fixed center.} Points are sorted by their angle from the
center. A partition is a choice of \code{k} cut-gaps among the \code{n} gaps
between consecutive points (\code{combn(n, k)} candidates); per-arc majority
counts are read from a cumulative count table and total misclassification
is minimised. Cut angles are placed at the arc midpoint between adjacent
points. Tie-breaker: among k-tuples with the same misclass count, the one
with the largest minimum 2D perpendicular distance from a cut ray to the
nearest point wins.

\strong{Search optimal center.} When \code{cx} and \code{cy} are \code{NULL} (default), the center
is optimised by multi-start Nelder-Mead — the brute-force above runs
as the inner objective at each candidate center. Starts are the data
centroid plus the centroid of each non-empty group. \code{parscale} is set
to the data range. When \code{cx} and \code{cy} are supplied, they are used
directly (no optimisation).
}
\examples{
\dontrun{
set.seed(123)
theta <- rep(c(0, 2 * pi / 3, 4 * pi / 3), each = 12) + rnorm(36, 0, 0.25)
r     <- runif(36, 0.5, 1.5)
crd   <- cbind(r * cos(theta), r * sin(theta))
grp   <- factor(rep(c("a", "b", "c"), each = 12))
plot(crd, asp = 1)
angularPartition(crd, grp)
}

}
