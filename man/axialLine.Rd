% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axial.R
\name{axialLine}
\alias{axialLine}
\title{Axial partition for exactly 2 groups using binary LDA}
\usage{
axialLine(
  crd,
  group,
  fill = FALSE,
  output = TRUE,
  col = "purple",
  cols = c("steelblue", "tomato"),
  lwd = 2,
  lty = 1,
  add = TRUE
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns (x, y).}

\item{group}{Factor, character, or integer vector with exactly 2 levels.}

\item{fill}{If \code{TRUE}, shade the two half-planes (default \code{FALSE}).}

\item{output}{If \code{TRUE} (default), return a list of line parameters.}

\item{col}{Separator line colour (default \code{"purple"}).}

\item{cols}{Length-2 fill colours; used when \code{fill = TRUE}
(default \code{c("steelblue", "tomato")}).}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{add}{If \code{TRUE} (default), add to existing plot; if \code{FALSE}, call
\code{plot()} first.}
}
\value{
If \code{output = TRUE}, a list with \code{partition} (\code{"axial"}), \code{slope},
\code{intercept}, \code{predicted} (LDA-predicted class factor), \code{pcoords} (the
input \code{crd}), \code{pgroup} (\code{group} coerced to factor), \code{misclass}
(integer), and \code{misclass_points} (data frame with columns \code{x}, \code{y},
\code{label} for each misclassified point).
}
\description{
Fits a linear discriminant analysis to two-group 2D data and draws the
classical LDA boundary — perpendicular to LD1 through the midpoint of the
class means — on the current plot (default).
}
\details{
When LD1 is along the x-axis (\code{abs(w[2]) < 1e-10}) the separator is
vertical: \code{slope} is returned as \code{Inf} and \code{intercept} carries the
x-position of the line.

\code{axialLine()} returns the classical LDA boundary
(closed-form: LD1 direction, midpoint of class means as cut) using
\code{lda()} from the MASS package, optimal under multivariate-normal condition with
equal covariances. The return value includes \code{predicted}, the LDA
class assignment per point.

\code{\link[=axialLines]{axialLines()}} with \code{k = 2}, in contrast, searches the full
space for the axial partitions with minimal misclassifications;
it may differ from \code{axialLine()}.
Use it when you want the empirically best linear separator;
use \code{axialLine()} when you want the classical LDA classifier.
}
\examples{
set.seed(1)
crd <- rbind(cbind(rnorm(15, -1), rnorm(15)),
             cbind(rnorm(15,  1), rnorm(15)))
grp <- factor(c(rep("a", 15), rep("b", 15)))
axialLine(crd, grp, fill = TRUE, add = FALSE)

}
\seealso{
\code{\link[=axialLines]{axialLines()}} for the empirically-optimal parallel-line
partition over both angle and cut position (any \code{k >= 2}).
}
