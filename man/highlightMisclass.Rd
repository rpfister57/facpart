% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{highlightMisclass}
\alias{highlightMisclass}
\title{Highlight misclassified points on the active plot}
\usage{
highlightMisclass(x, col = "red", pch = 4, cex = 1.5, lwd = 2)
}
\arguments{
\item{x}{A data frame with columns \code{x}, \code{y}, \code{label} (the \code{misclass_points}
element of a partition result), or the full partition result list.}

\item{col}{Marker colour (default \code{"red"}).}

\item{pch}{Point character for the marker (default \code{4}, an X).}

\item{cex}{Point size expansion factor (default \code{1.5}).}

\item{lwd}{Line width of the marker symbol (default \code{2}).}
}
\value{
\code{invisible(NULL)}, called for its side effect of drawing on the
active plot.
}
\description{
Overlays a marker on each misclassified point, using the \code{misclass_points}
data frame returned by \code{\link[=axialLine]{axialLine()}}, \code{\link[=axialLines]{axialLines()}}, \code{\link[=radialCircle]{radialCircle()}},
\code{\link[=radialCircles]{radialCircles()}}, \code{\link[=radialEllipse]{radialEllipse()}}, \code{\link[=radialEllipses]{radialEllipses()}}, or
\code{\link[=angularPartition]{angularPartition()}}. The full result list may also be passed directly;
\code{misclass_points} is extracted automatically.
}
\examples{
\dontrun{
set.seed(1)
crd <- rbind(cbind(rnorm(15, -1), rnorm(15)),
             cbind(rnorm(15,  1), rnorm(15)))
grp <- factor(c(rep("a", 15), rep("b", 15)))
res <- axialLine(crd, grp, add = FALSE)
highlightMisclass(res)
}

}
