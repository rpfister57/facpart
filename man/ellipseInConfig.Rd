% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ellipse.R
\name{ellipseInConfig}
\alias{ellipseInConfig}
\title{Fit and draw an ellipse around a 2D configuration}
\usage{
ellipseInConfig(
  crd,
  labs = NULL,
  mid = "centroid",
  typ = "maxdist",
  output = TRUE,
  col = "black",
  fill = NA,
  lwd = 2,
  lty = 2
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns (x, y).}

\item{labs}{Currently unused (kept for backward compatibility).}

\item{mid}{\code{"centroid"} (default) places the ellipse center at the data
centroid; \code{"nullnull"} forces the center to the origin (0, 0).
Only affects \code{"maxdist"}.}

\item{typ}{Fitting strategy: \code{"maxdist"} (default), \code{"bestfit"}, or
\code{"minbound"}.}

\item{output}{If \code{TRUE} (default), return a list describing the ellipse.}

\item{col}{Border colour (default \code{"black"}).}

\item{fill}{Fill colour, or \code{NA} (default) for no fill.}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{2}).}
}
\value{
If \code{output = TRUE}, a list with elements \code{cx}, \code{cy}, \code{a}, \code{b},
\code{angle} (in radians).
}
\description{
Three fitting strategies are available:
}
\details{
\itemize{
\item \code{"maxdist"} — covariance-based ellipse, scaled so all points lie inside
with a 5\% buffer. Semi-axes are \code{sqrt(eigenvalue) * k}, where \code{k} is the
maximum Mahalanobis distance from the center.
\item \code{"bestfit"} — minimises the sum of squared normalised elliptic distances
\code{sqrt((u/a)^2 + (v/b)^2) - 1} via BFGS, starting from the covariance
ellipse.
\item \code{"minbound"} — minimum-area enclosing ellipse via \code{\link[cluster:ellipsoidhull]{cluster::ellipsoidhull()}}.
}

The ellipse is drawn on the currently active plot window.
}
\examples{
\dontrun{
set.seed(1)
crd <- cbind(rnorm(30), 0.5 * rnorm(30))
plot(crd, asp = 1)
ellipseInConfig(crd, typ = "maxdist")
ellipseInConfig(crd, typ = "bestfit", col = "blue")
ellipseInConfig(crd, typ = "minbound", col = "red")
}

}
