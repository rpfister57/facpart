% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axial.R
\name{axialLines}
\alias{axialLines}
\title{Multi-group parallel-line partition (angle + cuts searched)}
\usage{
axialLines(
  crd,
  group,
  fill = FALSE,
  output = TRUE,
  col = "purple",
  cols = NULL,
  lwd = 2,
  lty = 1,
  n_angles = 180L
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns.}

\item{group}{Factor, character, or integer vector with \code{k >= 2} levels.}

\item{fill}{If \code{TRUE}, shade the \code{k} strips between consecutive lines.}

\item{output}{If \code{TRUE} (default), return a list of results.}

\item{col}{Separator line colour (default \code{"purple"}).}

\item{cols}{Length-\code{k} fill colours; auto-generated if \code{NULL}.}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{n_angles}{Number of angles in \verb{[0, pi)} to scan (default \code{180L},
approximately 1-degree resolution); LDA's direction is added as an
extra candidate.}
}
\value{
If \code{output = TRUE}, a list with:
\itemize{
\item \code{slope} — shared slope of the lines (\code{Inf} if vertical)
\item \code{intercepts} — \code{numeric[k-1]}, y-intercepts (or x-positions if vertical)
\item \code{angle} — winning direction in radians
\item \code{margin} — minimum perpendicular distance from the cut line(s) to the
nearest point
\item \code{misclass} — total misclassified points
\item \code{sector} — \code{integer[n]}, sector \verb{1..k} per point in input order
\item \code{majority} — \code{character[k]}, majority group per sector
}
}
\description{
Partitions a 2D configuration into \code{k >= 2} groups using \code{k-1} parallel
separating lines. Both the direction of the lines and their positions
are searched to minimise empirical misclassification:
}
\details{
\itemize{
\item \strong{Angle}: grid of \code{n_angles} equally-spaced angles in \verb{[0, pi)},
augmented by LDA's LD1 direction. For each angle \code{theta}, points are
projected onto \verb{(cos theta, sin theta)}.
\item \strong{Cuts}: for each angle, exact brute-force enumeration over all
\code{C(n-1, k-1)} ways to split the sorted projections into \code{k} segments.
}

The \verb{(angle, cuts)} combination with lowest total misclassification wins.
Tie-breaker: among configurations with the same misclass count, the one
with the largest minimum margin (perpendicular distance from a cut line
to the nearest point) is preferred — keeps cut lines visually away from
the data points.

For \code{k = 2}, \code{axialLines()} can differ substantially from \code{\link[=axialLine]{axialLine()}}:
the latter places the cut at the midpoint of class means on the LD1
projection (classical Bayes rule under normality), while \code{axialLines()}
searches the full \verb{(angle, cut)} space for the empirical optimum.

Vertical-line guard: when the winning direction has \verb{w[2] ~= 0}, the
separators are vertical. \code{slope} is returned as \code{Inf} and \code{intercepts}
carry the x-positions of the lines.
}
\examples{
\dontrun{
set.seed(1)
crd <- rbind(cbind(rnorm(10, -1), rnorm(10)),
             cbind(rnorm(10,  0), rnorm(10)),
             cbind(rnorm(10,  1), rnorm(10)))
grp <- factor(c(rep("a", 10), rep("b", 10), rep("c", 10)))
plot(crd, asp = 1)
axialLines(crd, grp, fill = TRUE)
}

}
\seealso{
\code{\link[=axialLine]{axialLine()}} for the classical LDA Bayes-rule boundary
(closed-form, binary only). Use \code{\link[=axialLine]{axialLine()}} when you want the
statistical LDA classifier; use \code{axialLines()} when you want the
empirically optimal parallel-line partition.
}
