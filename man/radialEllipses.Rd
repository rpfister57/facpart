% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/radial-ellipse.R
\name{radialEllipses}
\alias{radialEllipses}
\title{K-group radial-elliptic partition (nested ellipses)}
\usage{
radialEllipses(
  crd,
  group,
  ellipse = NULL,
  fill = FALSE,
  output = TRUE,
  col = "purple",
  cols = NULL,
  lwd = 2,
  lty = 1,
  add = TRUE
)
}
\arguments{
\item{crd}{Numeric matrix or data frame with exactly 2 columns.}

\item{group}{Factor with \code{k >= 2} levels.}

\item{ellipse}{Optional length-5 numeric vector \verb{(cx, cy, a, b, angle)}
specifying the innermost ellipse exactly. When supplied, all outer
ellipses are uniform scalings of this one; only the scale factors
are searched.}

\item{fill}{If \code{TRUE}, shade each elliptic sector.}

\item{output}{If \code{TRUE} (default), return results list.}

\item{col}{Ellipse border colour (default \code{"purple"}).}

\item{cols}{Length-\code{k} colours; auto-generated if \code{NULL}.}

\item{lwd}{Line width (default \code{2}).}

\item{lty}{Line type (default \code{1}).}

\item{add}{If \code{TRUE} (default), add to existing plot; if \code{FALSE}, call
\code{plot()} first.}
}
\value{
If \code{output = TRUE}, a list with vectors \code{cx}, \code{cy}, \code{a}, \code{b},
\code{angle} (radians), \code{misclass} (list with \code{n} and \code{indices}),
\code{misclass_points} (data frame with columns \code{x}, \code{y}, \code{label} for each
misclassified point), \code{sector}, and \code{majority}.
}
\description{
Generalises \code{\link[=radialEllipse]{radialEllipse()}} to \code{k >= 2} groups using \code{k-1} nested
ellipses. The nesting constraint (ellipse \code{s-1} lies inside ellipse
\code{s}) is enforced during optimisation. Groups are ordered by factor
level (level 1 = innermost).
}
\details{
When \code{ellipse} is supplied as a length-5 vector \verb{(cx, cy, a, b, angle)},
it defines the innermost ellipse exactly. All \code{k-1} ellipses then share
that center, orientation, and a:b ratio — outer ellipses are uniform
scalings of the inner one. The scale factors \verb{t_2, ..., t_\{k-1\}} (with
\verb{t_s > t_\{s-1\} >= 1}) are found by exact 1D scan over each point's
critical scale \code{sqrt((u_i/a)^2 + (v_i/b)^2)} in the ellipse's rotated
frame.

When \code{ellipse} is \code{NULL} (default), each ellipse has its own
independently optimised center, semi-axes, and rotation, fitted by
Nelder-Mead. Multi-start uses the covariance ellipse of the inner
groups plus an inflated copy of the previous ellipse (a guaranteed-
feasible starting point that avoids stalling on the infeasibility
penalty plateau).
}
\examples{
\dontrun{
set.seed(1)
g1 <- cbind(rnorm(15, 0, 0.2), rnorm(15, 0, 0.15))
th2 <- runif(15, 0, 2 * pi)
g2 <- cbind(1.4 * cos(th2), 1.0 * sin(th2)) + matrix(rnorm(30, 0, 0.1), 15)
th3 <- runif(15, 0, 2 * pi)
g3 <- cbind(2.8 * cos(th3), 2.0 * sin(th3)) + matrix(rnorm(30, 0, 0.1), 15)
crd <- rbind(g1, g2, g3)
grp <- factor(c(rep("a", 15), rep("b", 15), rep("c", 15)))

# Independent ellipses
radialEllipses(crd, grp, fill = TRUE, add = FALSE)

# Fixed-shape mode: supply the innermost ellipse as a 5-vector
radialEllipses(crd, grp, ellipse = c(0, 0, 0.3, 0.2, 0), fill = TRUE, add = FALSE)
}

}
